[package]
name = "scheduler"
description = "kube-scheduler equivalent: filter/score plugin framework, default plugins, HTTP extender seam (T3.2, Q23)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
# Q23: reuse the controllers crate's client-go machinery (Client/Informer/
# WorkQueue/LeaderElector) instead of a third framework copy.
controllers = { workspace = true }
storage = { workspace = true }
common = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
# Integration tests stand up a stub HTTP extender (G23's python server,
# in-process variant).
axum = { workspace = true }
